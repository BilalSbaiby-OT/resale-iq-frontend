# Infrastructure (frontend)

Coolify on Hetzner is canonical. Next standalone output (`output: "standalone"`).
`BACKEND_URL` is `http://backend:8080` in the compose network, `http://localhost:8080` locally.

Do not follow Desktop `COOLIFY-DEPLOY.md` tunnel instructions — they are obsolete.
Live URL: https://resaleiq.dev
