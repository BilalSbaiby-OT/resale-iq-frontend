# WORKBOARD — the only place work is tracked

**Created 2026-09-01 because the company produced 27,466 lines of documentation and 2,084 lines of
product code in one day.** Thirteen to one. The founder noticed before I did: *"we have 5
departments and 21 agents and the product didnt change much at all."*

## The cause, and it is structural

**Eight of 21 agents cannot change anything.** `monetization`, `finance-ops`, `ux-researcher`,
`legal-compliance`, `verifier`, `data-scientist`, `security-eng` and `tech-lead` hold read and write
grants only — by design, and correctly, since an auditor that edits the thing it audits is not an
auditor.

**But nothing converted their findings into shipped change except the CEO happening to notice.** So
their entire output was prose. `monetization` found the defect that was blocking revenue and wrote it
in a document. It shipped eight hours later, and only because I read the document.

## The rule

**An analysis agent's deliverable is a ROW ON THIS BOARD, not a document.** A finding with no row is
not delivered. A document may exist as evidence; the row is the deliverable.

**Every row names a DOER who can actually make the change.** If no doer is named, the finding is
CEO-blocked and that is my failure, not the finder's.

**The CEO's job is to keep this board moving, not to read documents.**

**NOTE, `legal-compliance`, 2026-09-01: this file is under heavy concurrent-write contention
tonight — `W21`–`W25` now each appear twice with slightly different text below, from the same
agent's own retries racing each other, not from two different agents. Not attempting to dedupe
someone else's in-flight work. This pass's three new rows are `W40`–`W42`, checked unused against
this exact read.**

| # | Finding | Found by | DOER | Status |
|---|---|---|---|---|
| **W1** | **E2 — registering makes the product WORSE.** Anonymous sees `buy_below` + `sell_avg`; logged-in free sees **less** for the same query (`routes.py:975-995` vs `1022-1039`), while the only CTA on every result page is "Unlock the rest →" pointing at `/register` | `ux-researcher` → `monetization` | `backend-eng` | **CLOSED** — verified live pre-fix by driving the real `/auth/register` endpoint end-to-end and diffing the JSON (`buy_below`/`sell_avg` present anonymous, absent post-registration, still absent after attempting an unlock). Root cause: the free-logged-in redaction (`d74c2ac`, 2026-08-05) predates the anonymous teaser (`f67b7de`, 2026-08-22, "P1-2"), which was bolted on top of `_gate()` without reconciling the branch below it — an accident of build order, confirmed the anonymous side is the newer feature. Fixed in `demand-intel@5019fa0` (branch `claude/backend-eng/w1-free-teaser-parity`, not pushed/merged — CEO to land): free logged-in now gets AT LEAST what anonymous gets (`buy_below`, `sell_avg`, `sell_median`, `active_listings`); only depth (`sell_through_rate`, `top_sizes`, `size_velocity`, `opportunity_score`, `reasons`, `months_supply`) stays behind a plan or a spent unlock. `upgrade_url` for the free-logged-in teaser now points at `/stripe/plans`, not back at `/register`. 1276 tests pass (1274 baseline + 2 new: anon/free parity assertion, NULL-plan fail-closed regression), zero regressions |
| **W2** | **`/flip`: 136 of 157 URLs, and all 10 `/category`, have never earned one Google impression** | `seo` | `seo` | **CLOSED — `30e5542`.** 36 zero-model URLs killed; `zero_model_urls` counter at 0. `seo` **refused** the wider kill on live Index Coverage: of the remaining 129, half are indexed and only 9.3% carry a rejection signal. My premise (excluded-as-thin) was wrong. || **W12** | **TWO POSTS PUBLISHED, aggregate-only, no per-model price.** `twitter.com/ResaleIQdev/status/2094748731514188031` (Nike concentration) and `.../2094758863832842553` (New Balance demand, n=309). Spanish version queued. **The pipeline is proven end to end for the first time.** | CEO | CEO | **CLOSED** |
| **W18** | **REDDIT IS UNREACHABLE FROM THIS ENVIRONMENT — the 10k-views plan has to change.** `legal-compliance` made eleven fetch attempts across five techniques (direct, `old.reddit.com`, `.json`, `.rss`, a text-extraction proxy, two Redlib mirrors, `web.archive.org`) and every one was blocked, with `example.com` as a working control. I then confirmed it independently three more ways: `WebFetch` blocked, the **Browser pane refuses `old.reddit.com` by policy**, and the **egress rail blocks the OAuth POST** because reddit.com is not on the allowlist. **Read-only `.json` also returns nothing parseable.** So we cannot read the rules, and we could not post even if we had. **This is not a refusal to work — it is four independent confirmations of an environment block.** | `legal-compliance` → CEO | CEO | **CLOSED as BLOCKED** — Reddit is out. **The 10k target now rests on TikTok and Instagram**, which are algorithmic rather than follower-gated and which we CAN post to through Postiz. Widening the egress allowlist for Reddit is a founder security decision, not a marketing convenience, and I am not taking it unilaterally |
| **W54** | **🔴 THE DEPLOY KEY'S FORCED COMMAND STILL ISSUES A GET.** Coolify changed `/api/v1/deploy` to require **POST**; the `authorized_keys` forced command behind `COOLIFY_DEPLOY_KEY` was not updated, so **every backend deploy returns 405 and queues nothing.** Confirmed: a GET returns *"This endpoint has changed to a POST request"*, and Coolify's deployment history for the app had **zero rows.** The backend served a 2-hour-old commit while four merged fixes — including a **live paid-field leak** — sat unshipped behind a **green** Deploy | CEO | `devops` | **OPEN.** I deployed manually via POST (backend now on `d44aae2`) and made the workflow **verify the commit** rather than accept any 200. **But pushes will keep no-opping until the forced command is changed to POST.** `authorized_keys` is security-sensitive and belongs to `devops`, not to a CEO mid-sweep |
| **W24** | **Every deploy still drops SOME requests for under ~2s — root-caused, not a full outage.** Read-only `ssh`+`docker`+Postgres investigation on the live Hetzner box (nothing restarted/reconfigured), then measured live across 2 real production deploys today (13:53-13:57 and 14:03-14:07, ~460 polls of `/` at 200-500ms, plus a parallel `docker ps` tracker via ssh). **Mechanism is NOT "Coolify rebuilds in place."** Coolify (v4.3.11) does a real create-new→wait-for-health→remove-old rolling swap at the container level (`ApplicationDeploymentJob.php: rolling_update()`), and it's already correctly gated for both apps because Coolify auto-detected the Dockerfile's own `HEALTHCHECK` (`custom_healthcheck_found=true` in Postgres for app id 2 and 3) — directly observed: new frontend container created 14:05:45, old one (up since 14:02:15) not removed until ~14:06:26, a real ~41s two-container overlap, matching `health_check_start_period`=40s. **The actual gap is one layer up: the proxy is Traefik v3.6 (not Caddy, despite `caddy_*` labels also being present — `caddy` binary isn't even installed in that container), and Coolify NEVER emits a Traefik `loadbalancer.healthcheck` label for any app in any settings state** — confirmed by reading `fqdnLabelsForTraefik()` (265 lines, zero health-related output) on the running Coolify instance, not inferred from docs. So Traefik adds the new (not-yet-warm) container to the same load-balanced service as the old one the instant it's created, and round-robins between "old, good" and "new, not ready yet" — this is exactly the CEO's original catch: `/`,`/es`,`/fr`=000 while `/de`,`/it`,`/pt`=200 *at the same instant*, which is a routing artifact, not a site-wide outage. **Measured window:** every single 000 across ~460 polls was an isolated single-poll hit — never two consecutive failures at 200-500ms cadence, so each failed request's own downtime is under ~1-2s. Failures cluster more densely inside the ~40-60s container-overlap period per deploy (2 of the 000s landed inside the one cleanly-isolated 41s overlap I captured) but a few also occurred with zero container churn on either app detectable at the time — so deploy activity provably worsens this, but a small baseline rate of blips against this host may not be zero even between deploys; reported as measured, not rounded up to a clean story. | CEO (measured live) → `devops` (root-caused + re-measured 2026-09-01) | **founder** (Coolify UI, see fix below) | **OPEN — no checkbox exists for this** (verified, not assumed: `health_check_enabled` in Coolify's DB only gates *Coolify's own* old-container removal, which — per `custom_healthcheck_found=true` — was already working correctly before this investigation; it never touches Traefik). **Cheapest real fix: a Traefik `retry` middleware via Coolify's existing "Custom Labels" field (Application → Advanced → Custom Labels) on BOTH apps** — this makes a request that lands on the not-yet-ready new container transparently retry against the still-alive old one, fixing the symptom regardless of exact cause. Frontend (app `p6t87shftvl2455pd3gdgee1`, add + attach to all 4 routers `http-0`/`http-1`/`https-0`/`https-1`): `traefik.http.middlewares.p6t87-retry.retry.attempts=3` / `traefik.http.middlewares.p6t87-retry.retry.initialinterval=200ms`, then each router's existing `.middlewares` label needs `p6t87-retry` appended (e.g. `traefik.http.routers.https-0-p6t87shftvl2455pd3gdgee1.middlewares=gzip,p6t87-retry`). Same pattern for backend (app `ph5clxk9hmghspv65pdkvak9`, router `http-0-ph5clxk9hmghspv65pdkvak9`) — the frontend calls the backend over this SAME Traefik-fronted internal hostname (`BACKEND_URL=http://ph5clxk9hmghspv65pdkvak9.62.238.51.83.sslip.io`), so a backend deploy can produce the exact "Could not check that item right now" `content-social` hit mid-capture. **This is a Coolify UI change I cannot make** — no API token reachable from outside localhost on this box, and writing Coolify's Postgres directly to inject the label bypasses Coolify's own reconciliation and is the kind of unreviewed prod reconfig this investigation was explicitly told not to do. Custom-label override of an already-generated `.middlewares` key is Coolify's documented merge behavior but I have not verified last-wins ordering against generated labels live — **after the founder applies it, `devops` should re-run this same poll+`docker ps` correlation on the next deploy to confirm zero 000s before calling it closed.** Blue/green is NOT needed — a checkbox-adjacent label is |
| **W25** | ~~STRIPE IS IN TEST MODE~~ — **RETRACTED. I WAS WRONG.** | CEO | `CEO` | **CLOSED — the alarm was false and the error was mine.** I queried Stripe with the **agent credentials file's** key and assumed it was production's. It is not: that key belongs to **`acct_1TmFnX…` "Demand Intel sandbox"** — a separate sandbox account, which is **correct hygiene** (agents should not hold a live key). **PRODUCTION IS LIVE.** Read from inside the running container: the key is `sk_live_…`, account **`acct_1TmFnC1Mvj7CL8HQ` "Demand Intel"** — the same one in the founder's browser — with `charges_enabled: true`. **We can take money and always could.** **And it confirms the founder's own account of history from primary source:** the live account holds **exactly 1 charge, paid and refunded** — his €19 Stripe test — and **0 active subscriptions**. **The classic error I have been catching in others all day: a confident claim from an unverified source.** I read one key and assumed it was the one that matters. |
| **W45** | **🔴 THE FIRST REAL FUNNEL MEASUREMENT — and the drop-off is registration.** 24h, non-bot, from production `pageviews`: **28 distinct visitors · 4 from social (3 Instagram, our reel) · 3 reached `/register` · 0 signed up.** `users` = **0 in 24h, 6 all-time**; `signup_attribution` = **0 rows ever**, so no signup has ever been attributed to a source. **Three people walked to the door and none opened it.** That is the single most actionable number this company has, and it is the first time any of it has been measured rather than assumed | CEO | `frontend-eng` | **OPEN — highest-value row on the board.** Walk `/register` as a stranger and find why 3 of 3 bounced. **WALKED LIVE 2026-09-01 by `ux-researcher`** — real browser session (`https://resaleiq.dev`, screenshots + accessibility-tree reads, not a code read), full account in `docs/company/ACTIVATION.md` §6. The homepage promise is kept: a fresh anonymous check returns a real live number (**BUY-BELOW €21** for Adidas Samba, **€37** for Nike Air Force 1, confirmed twice on two separate fresh tabs — not a stuck placeholder). The concrete, code-cited candidate for the bounce: the one CTA on every result defaults new signups into the €49/mo **Pro** plan, not Free — see **W47**. The withdrawal-waiver English line is confirmed live and matches what **W19** already recorded as by-design. Mobile submit was inconclusive this session, flagged separately, not asserted as a defect — see **W48** || **W46** | **A BLOG POST IS OUR BIGGEST PAGE AND NOBODY KNEW.** `/blog/vinted-disputes-and-returns-sellers` took **187 of 322 pageviews in 24h — 58% of all traffic**, against 59 for `/`. **But only 28 distinct non-bot visitors exist in the same window**, and blog referrer is 153×`None` / 1×google — so the count is inflated by repeat or unflagged-bot hits and **the true human figure is much smaller.** Both facts matter: the blog may be a real channel nobody is working, **or** our bot filter is leaking. **Do not act on 187 until that is separated** | CEO | `data-eng` | **OPEN** || **W47** | **The single CTA off every free check defaults new signups into the €49/mo Pro plan, not Free.** Live-walked `resaleiq.dev` 2026-09-01 in a real browser session (two fresh tabs, screenshots taken): a first-time anonymous check on "Adidas Samba" returns a real live number, **BUY-BELOW €21** (confirmed against a second check, "Nike Air Force 1" → **€37** — a different, live number, not a stuck placeholder). The only CTA on that result, **"Unlock the rest →"**, is `<SmartCTA anonLabel=... authedHref="/verdict" />` (`src/components/tools/free-checker.tsx:388`) with no `anonHref` override, so it falls through to `src/components/smart-cta.tsx:20`'s default, `anonHref = "/register"` — no `?plan=` param. `src/app/(auth)/register/register-form.tsx:27-29` reads that param and defaults `plan` to `"power"` (Pro) whenever it is absent; Free sits third of three radios, unselected, and requires an active click to reach. **Confirmed live, not just in code:** a fresh tab, on the exact click-path a real visitor takes from the homepage checker, opened `/register` with the Pro radio filled and highlighted green, not Free. A stranger who just got a free, honest, no-account answer is met by a paid-tier signup form as the default state | `ux-researcher` (live walk, code-cited) | `frontend-eng` | **OPEN** — smallest fix: give `free-checker.tsx:388`'s `SmartCTA` an explicit `anonHref="/register?plan=free"`. Directly serves **W45**: 3 of 3 registrations this week bounced, and this is the first concrete, code-level candidate reason a live walk has found, not a guess |
| **W48** | **Mobile free-checker: 4 of 4 submit attempts in this session returned no result — flagged UNCONFIRMED, not a proven site defect.** Resized the Browser pane to mobile (375×812, Android UA) and repeated the exact interaction that succeeded 2/2 on desktop (type a model, submit) against `https://resaleiq.dev`, using four different input methods (raw-coordinate click, ref-based click, Return key ×2). Every attempt left the page in its pre-search state — screenshot-confirmed: the typed text stayed in the box, no BUY-BELOW card ever appeared — and every mobile interaction in this session was preceded by a tool-level `"Browser pane is currently hidden... computer timed out after 30s"` error that never occurred on desktop. **Cannot separate a real product bug from this session's own rendering/compositing problem** — needs a clean retest (fresh session or a real device) before it is treated as a finding. Flagging now because it is the exact surface Instagram traffic would hit first (mobile-first channel, and per **W45** 3 of 4 social arrivals this week came from the reel) | `ux-researcher` (live walk, inconclusive) | `frontend-eng` (retest on a clean mobile viewport or real device; root-cause only if it reproduces) | **OPEN — UNCONFIRMED, retest before acting** |
| **W51** | **`UNLOCK_HARNESS` sat on disk while I believed it was removed.** My `rm` was inside a command the **guard blocked before it ran**, so the harness was unlocked for several minutes. **The rail-verification script found it; I did not.** Second time today a guard false-positive nearly left something unsafe — the first blocked a commit message describing a rail | CEO | CEO | **CLOSED** — removed, and all four rails re-verified blocking (force-push, PROTECTED write, out-of-scope write, unknown-host egress). **Lesson: after any `rm .claude/UNLOCK_HARNESS`, VERIFY it is gone rather than assuming the command ran.** A blocked compound command runs none of its parts |
| **W49** | **🔴 THE EGRESS RAIL GIVES FALSE CONFIDENCE.** `POST_HOSTS_OK` is `localhost, 127.0.0.1, resaleiq.dev, 62.238.51.83` — **nothing else** — while the company POSTs daily to **Gemini, Postiz, Stripe and ElevenLabs.** Worse: the rule only matches `curl\|wget\| `security-eng` | **CLOSED — `8cbe95b`.** Allowlist now names the four APIs we actually POST to, and all four rails re-verified blocking. **The script-egress blind spot is unchanged and stated**, not papered over. | `security-eng` then `devops` | **OPEN.** Fix is both halves: **allowlist the hosts we actually use** so the rail is true, and **make the check see script egress** or state plainly that it cannot. A rail that only catches the careless path is worse than none, because it is trusted || **W50** | **VOICEOVER WORKS — the blocker was never the permission.** ~~Key scoped too narrowly~~ **corrected:** `text_to_speech` **was** enabled, exactly as the founder said. TTS returns **200 and 44KB of real MP3** with a known-public voice id. **The 404 was the VOICE, not auth — `ELEVENLABS_VOICE_ID` points at a voice that does not exist on this account.** | CEO | **founder** (one of two, both trivial) | **OPEN.** Either enable **`voices_read`** so I can list and pick, **or** paste the voice id from the ElevenLabs UI — a voice id is a public identifier, not a secret. Then every video gets narration and is republished || **W3** | **`resaleiq.com` is not ours, returns 200, someone else owns it.** Two queued posts pointed at it | CEO | **founder** | **OPEN** — buy it or accept the confusion |
| **W4** | **53 remaining queue drafts** carry retracted vocabulary and/or per-model prices | `content-social` | `content-social` | **CLOSED** — worked in a worktree (`claude/content-social/w4-rewrite-queue`, `resale-iq-growth`, no code diff — this was a data-only pass on the gitignored `data/growth.db`), applied directly to the live DB with row-level UPDATE/DELETE/INSERT and a pre-write drift check (none of the 53 target ids had moved since read). **29 rewritten** (ids 61-66, 73-84, 94-98, 100-105) to current `growth.db brand_stats` snapshot 14 numbers, `resaleiq.dev` links, "left the shelf"/"watched departures" vocabulary, no per-model figures — CTAs name the paid max-buy-price feature instead of teasing a number. **24 deleted as unsalvageable**, not rewritten: sourcing-list Balenciaga + 4 other per-model buy-below prices (ids 52-55, GTM §B3 + DATA_CONTRACT rule 4), New Balance 530 momentum (88-93, model-level, paid per `/api/trends`), Adidas Samba competition ratio (67-72, paid per DATA_CONTRACT intro), New Balance 9060 sell-through (85-87, paid per rule 4), Patagonia size_trap (56-60, paid per rule 4 "size data"). **4 new ES/FR rows added** (ids 106-109, X/x_thread, New Balance demand + Adidas concentration, vocabulary locked to `resale-iq src/lib/i18n.ts`) per the 71%-US/GB-impressions finding in `GTM.md`. Verified: automated regex scan of all 33 remaining drafts for "sold"-family words (incl. compounds like "outsold", caught and fixed post-first-pass), `resaleiq.com`, Balenciaga, and per-model price patterns — 0 hits; all `script` fields valid JSON; all 5 reddit rows carry a `review_note` target (`r/Flipping`) the publish path requires (`platformSettings()` throws without one — none of this DB's reddit rows, including outside this batch, had ever carried one); `npm test` 61/61 green in the worktree. All 33 remaining rows left `status='draft'` — none approved, scheduled or sent. **Found, out of scope for this row, flagged separately below:** a *published* Instagram reel (`content` id 51, live since 2026-08-30, `instagram.com/resaleiqx/reel/Dcq6Dq6tjGp/`) carries the same defects this row fixed in draft form — 5 brands' per-model buy-ceiling prices including Balenciaga, and "sold" language throughout. It predates this session and is not a draft, so it's outside W4; needs a founder/legal-compliance decision on takedown or edit. |
| **W5** | **`compute_authenticity_score` returns `market_avg_price`** — a paid field — to any authenticated user | `backend-eng` | `backend-eng` | **CLOSED — A24 in `APPROVALS.md`, merged `74c1d4f`.** `product-manager` ruled it a genuine leak: bypasses the `n≥8` floor **and** the free-unlock budget, and manual mode needs no listing. Score and label stay free; the raw number goes behind the paywall. || **W6** | **`ELEVENLABS_API_KEY` returns 401.** Voiceover dead; `ELEVENLABS_VOICE_ID` already set | CEO | **founder** | **OPEN** || **W7** | **Rotate `RESEND_API_KEY`** — reached a transcript today | `devops` | **founder** | **OPEN** |
| **W8** | **Video assets are B-roll, not marketing.** Need product-in-frame: listing in, verdict out, hook text frame one | **founder** | CEO | **CLOSED** — `docs/marketing/assets/samba-demo-9x16.mp4`, 1080×1920, 15.9s: hook card → the search typing out character by character → **BUY-BELOW €21 · MARKET €32 · LEFT SHELF 63 · STILL LISTED 19,016 · WATCH** → end card. **Captured live from production; no number composed.** Reproducible: `scripts/capture_demo.mjs` → `scripts/make_cards.mjs` → ffmpeg concat. `designer` could not do it — I gave it a browser and **no shell**, so `gen_video.py` was unreachable; **my briefing error**, and it reported the miss rather than faking an asset. Four obstacles solved rather than worked around: hidden Browser pane could not composite → headless Playwright; Playwright wanted an uninstalled headless-shell while four full Chromium builds sat cached → script **discovers** the newest; this ffmpeg has **no `drawtext`** → cards rendered in a browser, which uses the product's own tokens. Frames gitignored (134/run, build output). |
| **W9** | **`live-market-proof.tsx`, `extension-hero.tsx`, `watchedSampleNote()`** still English in all 5 markets | `frontend-eng` | `frontend-eng` | **CLOSED** — commit `8e04650` on `claude/frontend-eng/w9-i18n-surfaces` (worktree under the session scratchpad, not this checkout; rebased clean onto current `main`, not pushed). All three named surfaces now read `copy[locale]` (new `liveProof`/`extensionHero`/`watchedSample` keys in `i18n.ts`, six locales); `watchedSampleNote()` takes an optional `locale` (default `"en"` — the one other call site, the English-only dashboard `/verdict` page, is unaffected) and localises its two numbers via `toLocaleString` per market. Extension-hero's example domain now follows the served locale (`vinted.fr/.de/.it/.pt/.es`) instead of always showing `vinted.es`. No `sold`/`verkauft`/`venduto`/`vendido`/`vendu` anywhere in the diff — grepped clean. Brand-voice strings (the proof sentence, the plan-upsell paragraph, the extension caption) flagged inline as BRAND-VOICE vs FUNCTIONAL for native review; the condition label ("Very good" et al.) flagged as unverified against Vinted's own live condition-picker string per market — this pass had no way to browse live Vinted in each locale to confirm it. Extended `e2e/i18n-checker.spec.ts` (the existing locale spec, not a new mechanism) with 3 tests × 5 locales = **15 new tests**; full run **65/65 passed** (`smoke`, `customer-workflow`, `signup-verify`, `regression-p0`, `market-coverage`, `locale-routing`, `i18n-checker`). `tsc --noEmit` clean, `next build` clean. Verified with raw `curl` of `/es /fr /de /it /pt` and `/` (still English, hreflang untouched) — not just source-read. |
| **W10** | **Pricing proposal never implemented.** Recommendation was hold €19/€49 — but the *enforcement* half shipped and the *tier* half was never decided | `monetization` | `product-manager` | **CLOSED — A23 in `APPROVALS.md`, merged `74c1d4f`.** Hold €19/€49, no €99, reposition the pitch: `buy_below` is already free to anonymous visitors at 10/day, so the page must not imply you pay to see the price. || **W11** | **A live Instagram reel (`growth.db` `content` id 51, published 2026-08-30, `instagram.com/resaleiqx/reel/Dcq6Dq6tjGp/`) is already carrying the exact defects W4 removed from the draft queue** — per-model buy-ceiling prices for 5 brands including a Balenciaga authenticity-adjacent claim (`under €106.50, sells ≈€171`), and "sold" language throughout. Found while auditing W4; it predates this session and isn't a draft, so it sat outside that row's scope | `content-social` | `founder` | **CLOSED — the founder deleted the reel.** URL 302s; `growth.db` row 51 marked `deleted_by_founder`. I listed it as outstanding for hours after he had done it. || **W19** | **The entire post-click auth funnel is unlocalized.** `/es` `/fr` `/de` `/it` `/pt` landing pages are genuinely translated (`src/lib/i18n.ts`, `src/app/[locale]/page.tsx`), but every CTA off them (`landing-content.tsx`) points at bare English routes — `/register`, `/login`, `/support` — with no locale prefix or param. `(auth)/register/page.tsx`, `check-email`, `verify-email`, and the post-signup `(dashboard)/dashboard/page.tsx` are English-only, including the **EU 14-day-withdrawal-waiver legal checkbox** a paying signup must tick. For a Spanish (or FR/DE/IT/PT) reseller, localization stops at the exact moment of highest commitment — the single most likely reason for drop-off per `docs/company/ACTIVATION.md` §5 | `customer-success` (code read, browser walk blocked — see W21) | `frontend-eng` | **CLOSED — `d2e5a39`, 73/73.** Auth flow serves the visitor's language via the `NEXT_LOCALE` cookie `proxy.ts` already set. Waiver stays English by design, with tests pinning it. || **W20** | **`docs/company/GOALS.md:117` still states the false "1 customer ever, refunded and cancelled."** Corrected everywhere else it appeared (`PATH-TO-TEN.md`, `GTM.md`, `MONETIZATION.md`, `DIGEST-2026-09-01.md` — see `ACTIVATION.md` §4) with dated inline notes. `GOALS.md` was deliberately not touched: `DIGEST-2026-09-01.md` records it as sha256-hashed specifically so a later edit is detectable — an unannounced edit by this role would look identical to tampering with a pre-registered goal | `customer-success` | whoever holds the `GOALS.md` hash (CEO/`product-manager`) | **CLOSED.** The false 1-customer claim is corrected across `PATH-TO-TEN`, `GTM`, `MONETIZATION` and the digest. `GOALS.md:117` is sha256-pinned and left deliberately — changing a hashed file to fix a quote is the wrong trade. || **W21** | **The Browser pane is unusable for `customer-success` this session.** `tab-1` is pinned to a local file preview; every `navigate` (including via `browser_batch`, `resize_window`) fails with "Open a new tab with `tabs_create`" — a tool not present in this role's toolset. Retried 5× with different URLs, deterministic failure each time. Blocked the live verification this session was explicitly asked to do (walking `/es`, confirming W1 live) — substituted a static code read instead, clearly labelled as such in `ACTIVATION.md` §0 | `customer-success` | **devops/CEO** (fix the tab state or grant `tabs_create`) | **CLOSED.** `ux-researcher` found the workaround: `tabs_create` is reachable through `browser_batch`. It walked the live funnel with it and found W47. |
## Rules that keep this board honest

1. **A finding is not done when the document is written. It is done when the row closes.**
2. **Rows close on evidence** — a commit, a URL, a test count. Not on a claim.
3. **If a row has no doer, the CEO is the blocker.** Say so in the row rather than leaving it to look
   like the finder's problem.
4. **Documents are evidence, not deliverables.** 27,466 lines of prose shipped nothing on their own.

## Agents must not work in this checkout

Five incidents in one day traced to agents creating branches and worktrees inside
`/Users/bilalsbaiby/work/resale-iq` — the tree the CEO pushes from:

1. `git add -A` swept a nested worktree into a **pushed commit** as a gitlink.
2. `tsc` type-checked that worktree and failed on **its** unresolved imports — a broken signal
   that nearly read as a merge regression.
3. `designer`'s half-finished work sat in the tree and survived only on timing.
4. Three commits landed on `claude/content-social/queue-rewrite` while `git push origin main`
   reported **"Everything up-to-date"** — three deploys announced that had not happened.
5. Two more landed on `claude/backend-eng/w1-close-workboard`, same way.

| **W22** | **Traffic source breakdown is CONFLICTING and affects all visitor-based models by 25×.** GSC shows 6 clicks/month from organic search (2026-08-31 data, 906 impressions). Pageviews show ~150 visitors/month net of bots (2026-08-07→08-31). If organic is primary channel, 6/month is the floor. If other channels (direct, referral) carry traffic, 150 is relevant. UNIT-ECONOMICS.md §4 models 222 months to 100 customers at 150 v/mo; at 6/month it's 5,555 months. **This 25× gap must be reconciled before any visitor-based forecast is credible.** | `finance-ops` (identified gap) | `seo` or `analytics` | **CLOSED — `30e5542`.** Both figures were right about different things: GSC's 5 clicks is **organic only**; 198 distinct non-bot visitors over 25 days is **all channels** (75% direct, ~12% our own `/admin`). Answered to `finance-ops` on the bus. || **W23** | **Trial→paid rate is UNKNOWN (n=0); trial machine is 2–3× lever on timeline.** Company has never measured trial→paid because trial-expiry machine does not exist. PATH-TO-TEN.md assumes 5–15% from industry SaaS. UNIT-ECONOMICS.md §4 shows trial→paid is load-bearing: at 10% trial→paid, 100 customers needs 33k visitors (222 months); at 25% (if asks ship), only 13k visitors (89 months). **Shipping trial machine first = 2–3× improvement in timeline without any new traffic.** Code is ready per PATH-TO-TEN §2 item 8; needs founder approval to enable email sends. | `finance-ops` (identified lever) | `founder` | **CLOSED — `0c289c6`, 1291 tests.** The machine was already built; the gap was that **nothing proved the founder gate**. Two mutation-tested tests now do. Activation is one variable: `LIFECYCLE_EMAILS=1`. || **W15** | **PENDING and LIMIT_REACHED blockers hide 24% of traffic from conversion.** 19 of 78 requests silently fail (`PENDING`); another set show "NO DATA" with no upgrade CTA (`LIMIT_REACHED`). These artificially depress funnel metrics. Both ≤2h fixes already scoped. | `finance-ops` | `backend-eng` | **OPEN** — PATH-TO-TEN §2 items 3–4, scoped ≤2h each. Ship after traffic source reconciliation (W22) so baseline is clean. || **W43** | **Organic CAC is estimated at €100–150 at current burn, breakeven with 6-month LTV. Paid CAC > €200 will not pay back.** Decision framework: stay organic for next 6 months, measure LTV and churn, switch to paid only when LTV > €500 or visitors > 500/mo. | `finance-ops` | `finance-ops` | **OPEN** — UNIT-ECONOMICS.md §5 lays the case for 6-month organic-only strategy. Triggers for paid: LTV > €500 (measured), churn < 5%/mo, CAC < €100 available at scale. || **W44** | **100 customers timeline is 12–30 months on organic with trial machine + organic growth 2–3× + funnel fixes, NOT 4–6 months.** Central estimate: 33k visitors at current 150/mo (all channels) or 6/mo (organic only) = 222 months or 5,555 months. Reconcile traffic source first (W22). Timeline improves to 89 months if trial→paid reaches 25% (trial machine ships, asks turn on). Further improvements require organic growth 2–3× (TikTok/SEO) and PENDING/LIMIT_REACHED fixes. | `finance-ops` | `finance-ops` | **OPEN** — UNIT-ECONOMICS.md §7 documents why founder's 4–6 month estimate is unachievable without measured levers. Depends on: (1) traffic source clarification (W22), (2) trial machine ship (W23), (3) organic growth 2–3× (in flight: TikTok, SEO), (4) funnel fixes (W15). || **W40** | **Even setting the Reddit egress block (W18) aside, `legal-compliance` has no way to read `resale-iq-growth/data/growth.db`.** `growth.db` is a binary SQLite file; this role's toolset (`Read`/`Write`/`WebSearch`/`WebFetch`/`Grep`/`Glob` + a browser pane that cannot navigate — same wall as W21) has no `sqlite3`/Bash, no export exists in `data/exports/`, and the local dashboard API (`src/server/index.js`, port 4310) is unreachable without a working browser. The five queued Reddit rows' content-honesty is relayed-clean via `content-social`'s dated regex scan (see `W4`: 0 hits for "sold"-family words, `resaleiq.com`, Balenciaga, per-model prices, across all 33 drafts including these 5) — but that is `content-social`'s evidence, not this role's own confirmation, and it currently cannot independently check any DB-backed content queue at all, for Reddit or any future channel | `legal-compliance` | `devops` | **OPEN, not urgent tonight** (Reddit is blocked anyway per W18) — but standing: any future "check this batch of drafts" ask hits the same wall || **W41** | **`COMPLIANCE.md` finding #1 — the GDPR export/erasure gap — re-confirmed live today, unchanged from this morning's audit.** `delete_account` (`demand-intel/api/auth.py:842-851`) still issues only `DELETE FROM users WHERE id=?`, no cascade, no `verdict_logs`/`purchase_logs` cleanup, no Stripe cancellation; `export_data` (`:1120-1142`) still unions nothing beyond `users`+`activity_logs` while claiming in its own response body to hold "all personal data... per GDPR Article 20"; `/privacy` still promises both. **This is this week's stated company goal** ("deletion cascades, `verdict_logs` is included, Stripe is cancelled" + a DSAR runbook run end to end) **and none of it has shipped yet** | `legal-compliance` (re-confirmed against current code, not on trust) | `backend-eng` ships `COMPLIANCE.md §1.5` steps 2-4 | **OPEN — this is the week's goal, unmet as of this check.** Fix is already fully specified in `COMPLIANCE.md §1.5` — nothing left to design. Cheap interim if the full fix slips: soften the two GDPR-promise lines on `/privacy` today, no code change, so the live page stops promising what the backend doesn't do while the real fix is built |
| **W42** | **New `/privacy` extension-telemetry paragraph (added today, "Last updated: 1 September 2026") checked against the actual extension code and the Chrome Web Store listing draft: accurate, consistent across all three surfaces, no PII, and currently collecting nothing at all** — the backend has no `/api/ext/error` route yet (the extension's own code comment says the POST 404s quietly). The one clean result of this pass | `legal-compliance` | `backend-eng` | **OPEN, low urgency — not a violation today.** Becomes one only if the future backend route collects more than disclosed, or the CWS console diverges once v1.3.0 actually goes live || **W52** | **Two false marketing claims, still live, found by `PRICING-PROPOSAL.md` §1.2 and independently re-confirmed against code:** "100 product signals, unblurred" (`src/lib/pricing.ts:64-73` — `VerdictResult` actually carries ~25 fields, `docs/audit/CLAIMS.md` §2, no "100" count exists anywhere in either repo) and "scraped every 30 min / recomputed hourly" (`docs/audit/CLAIMS.md` §3 — real cadence is 2 hours per `config.py`, because production is 403-blocked by Vinted and a residential Mac agent does the real scraping). Both are on the pricing page, the FAQ, the extension store listing and `llms.txt` today. **Must be fixed before the W10/A23 pitch reposition ships** — new honest positioning copy next to two still-false feature claims repeats the exact defect A23 exists to close | `monetization` (found, in `PRICING-PROPOSAL.md`) | `content-social` | **OPEN** — not urgent standalone, but blocks W10's pitch copy from shipping honestly. Four surfaces to correct: pricing page, FAQ, extension store listing, `llms.txt`. |
**Rule: every agent works in a worktree under the session scratchpad, never in this repo.**
**And the CEO verifies `git rev-parse --abbrev-ref HEAD` before every push, and confirms the
remote moved afterward** — `git push` reporting success is not evidence that anything shipped.
